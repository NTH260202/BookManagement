package view;

import java.util.Scanner;

import controller.BookDataController;
import model.Book;

public class View {
    public void View() {
        int choice = 0;
        Scanner scanner = new Scanner(System.in);
        do {

        } while (choice != 0);
    }
}
